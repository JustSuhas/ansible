- name: Variables from files
  hosts: local
  connection: local
  vars_files_ref:
    - var_file.yaml
  tasks:
  - name: print from var_file
    ansible.builtin.debug:
      msg: "Course name is: {{ course }}, by trainer: {{ Trainer }}"